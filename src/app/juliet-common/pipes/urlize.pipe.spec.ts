/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { UrlizePipe } from './urlize.pipe';

describe('UrlizePipe', () => {
  it('create an instance', () => {
    let pipe = new UrlizePipe();
    expect(pipe).toBeTruthy();
  });
});
