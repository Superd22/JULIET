import { BlueResizableDirective } from './blue-resizable.directive';

describe('BlueResizableDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueResizableDirective();
    expect(directive).toBeTruthy();
  });
});
