import { AlignShipIcoDirective } from './align-ship-ico.directive';

describe('AlignShipIcoDirective', () => {
  it('should create an instance', () => {
    const directive = new AlignShipIcoDirective();
    expect(directive).toBeTruthy();
  });
});
