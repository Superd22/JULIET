import { TargetListFilterPipe } from './target-list-filter.pipe';

describe('TargetListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TargetListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
